import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    padding: '12px 25px',
  },
  title: {
    textAlign: 'center',
  },
  groupTitle: {
    width: '100%',
  },
  members: {
    marginTop: '70px',
  },
});

export default useStyles;
